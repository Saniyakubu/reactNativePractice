import { View, Text, SafeAreaView } from "react-native";

const Header = () => {
  return (
    <SafeAreaView style={{ flex: 1, backgroundColor: "white" }}>
      <View style={{ backgroundColor: "white", height: 60 }}>
        <Text style={{ color: "black" }}>hshhhhhhh</Text>
      </View>
    </SafeAreaView>
  );
};

export default Header;
